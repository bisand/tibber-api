
export enum GQL {
    CONNECTION_INIT = 'connection_init',
    CONNECTION_ACK = 'connection_ack',
    CONNECTION_ERROR = 'connection_error',
    SUBSCRIBE = 'subscribe',
    COMPLETE = 'complete',
    NEXT = 'next',
    ERROR = 'error',
}
